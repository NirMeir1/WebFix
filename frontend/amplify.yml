version: 1
applications:
  - appRoot: frontend
    backend:
      phases:
        build:
          commands:
            - npm ci --cache .npm --prefer-offline
            - npx ampx pipeline-deploy --app-id $AWS_APP_ID --branch $AWS_BRANCH
    frontend:
      phases:
        build:
          commands:
            - npm ci --cache .npm --prefer-offline
            - npm run build
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths:
          - .npm/**/*